import { Typography } from "@mui/material";

export const HomePage = () => {
    return (
        <>
         <Typography variant="h1">HomePage</Typography>       
         <Typography variant="h1">HomePage</Typography>
         <Typography variant="h1">HomePage</Typography>
        </>
    );
}
